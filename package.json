{
  "name": "run-alert",
  "version": "0.1.0",
  "description": "러닝 대회 일정과 러닝화 발매 정보를 매일 수집해 신규 건만 알려주는 자동화",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "collect": "node src/collect.js",
    "collect:dry": "node src/collect.js --dry",
    "probe": "node tools/probe.mjs",
    "server": "node src/server.js",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "express": "^4.19.2",
    "fast-xml-parser": "^4.4.1",
    "playwright": "1.56.0"
  },
  "devDependencies": {
    "@playwright/test": "1.56.0"
  }
}
